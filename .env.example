# Only required when an evaluation request is not already cached.
OPENAI_API_KEY=

# Optional evaluation proxy overrides.
EVALUATION_PORT=8787
EVALUATION_UPSTREAM_URL=https://api.openai.com/v1
# Set this only if Lvce Editor is served from a non-local origin.
EVALUATION_ALLOWED_ORIGIN=
